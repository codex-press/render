{
  "codex": {},
  "scripts": {
    "start": "babel src -w -d ../server/render/build"
  },
  "babel": {
    "presets": [
      "env"
    ]
  },
  "dependencies": {
    "dateformat": "^1.0.12",
    "handlebars": "^4.0.5",
    "marked": "^0.3.6",
    "prismjs": "^1.5.1"
  },
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-env": "^1.6.0"
  }
}
